import React, { Component } from "react";
import {
  View,
  Text,
  FlatList,
  ActivityIndicator,
  StyleSheet,
  SafeAreaView,
} from "react-native";
import { ListItem, SearchBar, Avatar } from "react-native-elements";

const routes = () => {
  return (
    <View>
      <Text> i am the route</Text>
    </View>
  );
};

export default routes;
